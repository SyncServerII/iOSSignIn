import SwiftUI

struct MainScreen: View {
    var body: some View {
        VStack {
            Button(action: {
                // TODO
            }) {
                Text("Sign-in")
            }
            
            Button(action: {
                // TODO
            }) {
                Text("Create Account")
            }
        }
    }
}

struct MainScreen_Previews: PreviewProvider {
    static var previews: some View {
        MainScreen()
    }
}

